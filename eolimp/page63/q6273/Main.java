package page63.q6273;

import java.util.Scanner;

public class Main {
  public static void main(String[] args) {
    Scanner in = new Scanner(System.in);
    System.out.println((int)Math.sqrt(Math.pow(in.nextInt(), 2) +
                                      Math.pow(in.nextInt(), 2) +
                                      Math.pow(in.nextInt(), 2)));
  }
}
